import { describe, it } from 'vitest';

describe('Basic Marketplace Service', () => {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  it('should build transaction', () => { });
});
