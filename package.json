{
  "private": true,
  "name": "@workspace/root",
  "scripts": {
    "test": "lerna run test",
    "start": "lerna exec --scope @workspace/playground -- yarn dev",
    "build": "lerna link && yarn build:module && yarn build:demo",
    "build:demo": "lerna link && lerna exec --scope @workspace/playground -- yarn build",
    "build:module": "lerna link && lerna exec --scope @martifylabs/mesh -- yarn build"
  },
  "devDependencies": {
    "@types/jest": "28.1.6",
    "@types/node": "18.0.1",
    "lerna": "5.2.0",
    "prettier": "2.7.1",
    "typescript": "4.7.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
