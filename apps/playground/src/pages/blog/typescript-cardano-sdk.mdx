import LayoutImageHeaderAndBody from "~/components/layouts/image-header-and-body";
import { blogMesh20 } from "~/data/links-blogs";

export default function MDXPage({ children }) {
  return (
    <LayoutImageHeaderAndBody
      title={blogMesh20.title}
      description={blogMesh20.description}
      image={blogMesh20.image}
      cover={blogMesh20.cover}
    >
      {children}
    </LayoutImageHeaderAndBody>
  );
}

New article